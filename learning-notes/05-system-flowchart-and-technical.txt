NUTRISAFETY AI - FLOWCHART DAN PENJELASAN TEKNIS

1. Gambaran singkat
File ini menjelaskan alur kerja sistem, algoritma "AI" yang dipakai, dan cara kerja
teknis antar layer pada project NutriSafety.

2. Flow utama sistem
Alur sistem secara sederhana:

User input data menu di frontend
-> frontend memilih kategori bahan, skala gramasi, kelompok usia, budget,
   dan jumlah siswa
-> frontend menghitung preview total gizi, AKG, dan kebutuhan batch
-> frontend mengambil history menu lokal agar rekomendasi berikutnya tidak berulang
-> frontend membentuk payload JSON
-> request dikirim ke backend Express
-> backend memvalidasi data
-> backend meneruskan request ke AI Engine Flask
-> AI Engine memvalidasi ulang data
-> AI Engine mengambil profil AKG sesuai kelompok usia
-> optimizer membentuk model optimasi dengan constraint biaya, target gizi,
   dan struktur kategori menu
-> solver mencari kombinasi menu terbaik sambil menghindari menu history
-> hasil dihitung ulang: total gizi, AKG, biaya, status, kategori menu,
   dan kebutuhan bahan batch
-> hasil dikirim ke backend
-> backend mengirim hasil ke frontend
-> frontend menampilkan rekomendasi menu, pembanding manual vs optimasi,
   dan menyimpan history hasil terbaru

3. Flowchart versi teks
[Mulai]
  |
  v
[User memasukkan makanan, kategori bahan, harga, kandungan gizi,
 usia, budget, dan jumlah siswa]
  |
  v
[Frontend React hitung preview total gizi, AKG, dan kebutuhan batch]
  |
  v
[Frontend ambil riwayat menu dari localStorage]
  |
  v
[Frontend kirim POST /api/optimize ke backend]
  |
  v
[Backend Express validasi payload]
  |
  +-> jika tidak valid -> [Return error]
  |
  v
[Backend forward request ke AI Engine Flask]
  |
  v
[AI Engine validasi ulang]
  |
  +-> jika tidak valid -> [Return error]
  |
  v
[Ambil profil AKG berdasarkan age group]
  |
  v
[Bentuk model optimasi biner + daftar exclude dari history]
  |
  v
[Terapkan objective function dan constraints]
  |
  +-> biaya <= budget
  +-> kalori >= minimum otomatis dari AKG
  +-> protein >= minimum otomatis dari AKG
  +-> kategori inti menu: makanan pokok, lauk, sayuran
  +-> bonus skor untuk buah dan susu
  |
  v
[Solve dengan PuLP + CBC solver]
  |
  +-> jika infeasible karena history -> [Coba fallback tanpa history]
  |
  +-> jika tetap infeasible -> [Tidak ada kombinasi yang memenuhi syarat]
  |
  v
[Ambil solusi terbaik dan alternatif yang tidak duplikat]
  |
  v
[Hitung total gizi, persen AKG, biaya, kategori terpilih,
 status feasibility, dan kebutuhan bahan batch]
  |
  v
[Kirim hasil ke frontend dan simpan history terbaru di browser]
  |
  v
[Selesai]

4. "AI" yang dipakai sebenarnya apa?
Project ini tidak memakai machine learning, deep learning, atau model generatif.
Pendekatan yang dipakai adalah optimasi matematis.

Algoritma utamanya:
- Binary Linear Programming / Integer Linear Programming
- Library Python: PuLP
- Solver: CBC

Artinya, sistem memilih kombinasi makanan terbaik berdasarkan aturan matematika
dan batasan tertentu, bukan belajar dari data training.

5. Cara kerja algoritma optimasi
Setiap makanan direpresentasikan sebagai variabel biner:
- 1 = makanan dipilih
- 0 = makanan tidak dipilih

Sistem lalu mencari kombinasi makanan yang:
- memenuhi batas biaya
- memenuhi minimum kalori otomatis dari AKG
- memenuhi minimum protein otomatis dari AKG
- menjaga struktur menu inti: makanan pokok, lauk pauk, dan sayuran
- memberi skor gizi terbaik
- menghindari kombinasi menu yang baru saja dipakai jika memungkinkan

6. Objective function
Optimizer memaksimalkan nutrition score.

Rumus score yang dipakai:
score =
(protein * 5)
+ (calories * 0.03)
+ (fat * 0.7)
+ (carbs * 0.04)
- (price * 0.001)
+ bonus kategori

Maknanya:
- protein diberi bobot paling tinggi
- kalori, lemak, dan karbohidrat tetap dihitung
- harga menjadi pengurang kecil agar solusi tetap efisien secara biaya
- buah, susu, dan sayuran mendapat bonus agar komposisi menu lebih seimbang

7. Constraint yang dipakai
Model optimasi memiliki batasan:
- total biaya <= budget
- total kalori >= minimum_calories
- total protein >= minimum_protein
- minimal ada 1 makanan pokok jika kandidat tersedia
- minimal ada 1 lauk pauk jika kandidat tersedia
- minimal ada 1 sayuran jika kandidat tersedia

Jadi, tidak semua kombinasi makanan boleh dipilih.
Hanya kombinasi yang lolos constraint yang bisa jadi solusi.

8. Kenapa hasilnya bisa sampai 3 alternatif?
Sistem tidak berhenti di satu solusi.
Setelah solusi pertama ditemukan, kombinasi itu dimasukkan ke daftar exclude.
Lalu solver dijalankan lagi untuk mencari kombinasi lain yang berbeda.

Selain itu, sistem juga bisa menerima daftar menu history dari frontend.
Daftar ini ikut dimasukkan ke exclude agar hasil menu harian tidak berulang.

Dengan cara ini, sistem bisa memberi:
- alternatif rank 1
- alternatif rank 2
- alternatif rank 3

Setelah itu alternatif diurutkan berdasarkan:
- status kelayakan
- nutrition score
- total biaya

9. Penentuan status kelayakan
Hasil menu diklasifikasikan menjadi:

- Layak
  Budget aman, kalori >= 100 persen target makan, protein >= 100 persen,
  dan lemak serta karbohidrat >= 80 persen.

- Perlu optimasi
  Budget aman, tetapi target kalori atau protein belum benar-benar optimal.

- Tidak layak
  Kombinasi tidak memenuhi kriteria minimum.

10. AKG dipakai bagaimana?
Sistem punya profil AKG untuk beberapa kelompok usia:
- 7-9 tahun
- 10-12 tahun
- 13-15 tahun
- 16-18 tahun

Target makan dihitung sebagai 30 persen dari kebutuhan harian.
Jadi sistem mengevaluasi menu per porsi makan, bukan kebutuhan harian penuh.

11. Peran tiap layer
Frontend React:
- tempat user input data
- menampilkan dataset contoh
- menghitung preview total gizi, AKG, dan kebutuhan batch
- menyimpan history menu lokal
- mengirim payload ke backend
- menampilkan hasil optimasi

Backend Express:
- menerima request dari frontend
- memvalidasi payload
- menjadi API gateway ke AI Engine
- mengembalikan response hasil optimasi

AI Engine Flask:
- memvalidasi ulang request
- mengambil profil AKG
- menjalankan optimizer PuLP
- menghitung hasil akhir, kategori menu, dan ranked alternatives
- mengelola exclude list dari history dan fallback bila perlu

12. Struktur teknis sistem
Arsitektur project:
- Frontend: React + Vite
- Backend: Node.js + Express + Axios + CORS
- AI Engine: Python + Flask + PuLP

Pemisahan ini penting karena:
- UI tidak bercampur dengan logika API
- validasi request tidak bercampur dengan logika optimasi
- engine optimasi bisa dikembangkan terpisah dari frontend

13. File penting untuk dipelajari
- frontend/src/App.jsx
- frontend/src/data/foodCategories.js
- frontend/src/data/portionScales.js
- frontend/src/utils/nutrition.js
- backend/server.js
- ai-engine/app.py
- ai-engine/akg_profiles.py
- ai-engine/optimizer.py

14. Kesimpulan
NutriSafety adalah sistem rekomendasi menu berbasis optimasi matematis.
Sistem ini memilih kombinasi makanan yang paling baik berdasarkan gizi dan biaya,
dengan referensi target AKG per kelompok usia.

Jadi, "AI" di project ini lebih tepat disebut:
AI optimization engine, bukan AI machine learning.
